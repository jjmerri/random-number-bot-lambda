#############################
# Environment Variables
# Copy this file to `.env` and fill in real values for local development.
# The lambda in AWS gets these from SSM Parameter Store (see `serverless.yml`).
#############################

# Reddit bot credentials
REDDIT_USERNAME=your_bot_username
REDDIT_PASSWORD=your_bot_password
REDDIT_CLIENT_ID=your_reddit_app_client_id
REDDIT_CLIENT_SECRET=your_reddit_app_client_secret

# Email / SES (or other SMTP) credentials
EMAIL_SERVER=email.smtp.server
EMAIL_USERNAME=ses_smtp_username
EMAIL_PASSWORD=ses_smtp_password

# Dev / maintainer contact info
APP_DEV_EMAIL=dev@example.com
APP_DEV_USERNAME=DevRedditUser

# random.org API key
APP_RANDOM_DOT_ORG_API_KEY=your_random_org_api_key

# Notes:
# - For local runs you can: `export $(grep -v '^#' .env | xargs)` (zsh/bash)
#   or install python-dotenv and load automatically.
# - Do NOT commit the real `.env` file.